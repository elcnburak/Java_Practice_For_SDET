package Day04;

import java.util.Scanner;

public class Practice_22_Example {
    public static void main(String[] args) {

        // Enter name print to screen
        Scanner oku=new Scanner(System.in);
        System.out.println("Please enter your name");
        String name=oku.next();

        System.out.println("name = " + name);


        // oku.Next()        = Reads a single word
        // oku.NextLine()    = Reads the entire line
        // oku.NextInt()     = Reads an integer
        // oku.NextShort()   = Reads a short integer
        // oku.NextByte()    = Reads a byte integer
        // oku.NextLong()    = Reads a long integer
        // oku.NextFloat()   = Reads a decimal number
        // oku.NextDouble()  = Reads a long decimal number
        // oku.NextBoolean() = Reads a boolean value (true / false)
        
    }
}
